// program in C to add two integers
#include <stdio.h>
int main() 
{
  int a, b, z;
  printf("Enter two numbers ");
  scanf("%d%d", &a, &b);
  z = a + b;
  printf("Sum is %d", z);
  return 0;
}

